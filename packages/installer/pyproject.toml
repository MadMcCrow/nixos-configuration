[project]
name = "nixos-pyinstall"
version = "0.1.0"
description = "tool to install nixos from any configuration"
authors = [{ name = "MadMcCrow", email = "do-not-mail.me@please.com" }]
requires-python = ">=3.11"
readme = "README.md"
dependencies = ["pycall >= 0.0", "slog >= 0.0", "config", "install" ]

[project.scripts]
nixos-pyinstall = "cli:main"

[project.gui-scripts]
nixos-pyinstall-gui = "gui:nixos-pyinstall-gui"

[tool.uv]
compile-bytecode = true

[tool.uv.sources]
gui = { workspace = true }
config = { workspace = true }
install = { workspace = true }
slog = {  path="../slog", editable=true }
pycall = { path="../pycall", editable=true }

[tool.uv.workspace]
members = ["packages/*"]

[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"

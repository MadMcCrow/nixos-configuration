[project]
name = "install"
version = "0.1.0"
requires-python = ">=3.10, <4"
dependencies = ["pycall", "slog", "cli", "config"]

[tool.uv.sources]
cli = {workspace = true}
config = {workspace = true}
pycall = { path="../../../pycall", editable=true }
slog = { path="../../../slog", editable=true }

[tool.uv]
package = true

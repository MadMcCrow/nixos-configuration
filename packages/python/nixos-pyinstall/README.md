# nixos-pyinstall

python tool to install nixos configurations by formatting and calling the right system commands for you

## deps :
    - pycall to make system calls
    - nixos-pyconfig for parsing your systems configuration

if [ -z ${key} ]; then
  echo -e "\b" | ssh-keygen -C ${key} -f ${key} -q
  fi
    wget raw.githubusercontent.com/${flake}/blob/main/secrets/secrets.nix
    for i in secretsStr; do
    if [-z]
      agenix -d ${i} -i ${key}
    done

[project]
name        = "install"
version     = "0.0.0"
description = "install command for ostools"
requires-python = ">=3.14"
dependencies    = [
    "shellous >= 0.41.0",
    "aiofiles >= 25.1.0",
    "shared"
]
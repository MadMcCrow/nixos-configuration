[project]
name        = "shared"
version     = "0.0.0"
description = "shared"
requires-python = ">=3.14"
dependencies    = [
    "shellous >= 0.41.0",
    "aiofiles >= 25.1.0",
    "rich >= 15.0.0",
    "prompt-toolkit >= 3.0.0"
]
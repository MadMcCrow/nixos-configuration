[project]
name        = "commands"
version     = "0.1.0"
description = "commands"
requires-python = ">=3.14"
dependencies    = [
    "shellous >= 0.41.0",
    "rich >= 15.0.0",
    "aiofiles >= 25.1.0",
    "prompt_toolkit>= 3.0.0"
]
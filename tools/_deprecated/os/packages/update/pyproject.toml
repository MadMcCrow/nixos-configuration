[project]
name        = "update"
version     = "0.0.0"
description = "update command for ostools"
requires-python = ">=3.14"
dependencies    = [
    "shellous >= 0.41.0",
    "aiofiles >= 25.1.0",
    "shared"
]
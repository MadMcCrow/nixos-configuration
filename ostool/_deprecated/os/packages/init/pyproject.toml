[project]
name        = "init"
version     = "0.0.0"
description = "init config for ostool"
requires-python = ">=3.14"
dependencies    = [
    "shellous >= 0.41.0",
    "aiofiles >= 25.1.0",
    "shared"
]